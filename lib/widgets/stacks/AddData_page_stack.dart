import 'package:flutter/material.dart';

import '../AddData.dart';

class AddDataPageStack extends StatelessWidget {
  const AddDataPageStack({super.key});

  @override
  Widget build(BuildContext context) {
    return AddData();
  }
}
